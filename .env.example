# App
FLASK_ENV=development
PORT=5000
PROJECT_TAG=INH-Integrations
BOTS_DIR=./bots

# SMTP (para emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=__SET__
SMTP_PASS=__SET__
MAIL_FROM="In Houston AI <no-reply@inhoustontexas.us>"
MAIL_TO="ops@inhoustontexas.us"   # fallback si un bot NO define email.to

# Defaults de follow-up (se usan solo si el bot no define los suyos)
SEND_LINK_URL=https://multi-bot-inteligente-v1.onrender.com/actions/send-link
DEFAULT_BOT=whatsapp:+18326213202
DEFAULT_CHANNEL=sms               # sms | wa
DEFAULT_LINK=https://calendly.com/houston-school/30min
DEFAULT_REGION=US                 # normalización a E.164
